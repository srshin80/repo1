apiVersion: v1
kind: Service
metadata:
  name: my-sample-app   # base service 이름 (prefix는 자동 붙음)
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 32000   # 반드시 30000~32767 범위, 중복 없어야 함
